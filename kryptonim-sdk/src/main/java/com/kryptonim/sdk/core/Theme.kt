package com.kryptonim.sdk.core

/** An enumeration representing the theme for the transaction interface. */
public enum class Theme {
    /** Represents the Light theme. */
    LIGHT,

    /** Represents the Dark theme. */
    DARK
}
